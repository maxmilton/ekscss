{
  "name": "@ekscss/monorepo",
  "private": true,
  "scripts": {
    "____build": "yarn workspaces foreach --all --topological-dev run build",
    "___build": "yarn workspaces foreach -pti run build",
    "__build": "yarn workspaces foreach run build",
    "_build": "yarn workspaces foreach --topological-dev run build",
    "_buildp": "yarn workspaces foreach -pi --topological-dev run build",
    "build": "yarn workspaces run build",
    "lint": "yarn lint:md && yarn lint:css && yarn lint:js && yarn lint ts",
    "lint:css": "echo 'TODO'; exit 1",
    "lint:js": "eslint --ext .ts,.tsx,.js,.jsx,.mjs,.cjs,.md .",
    "lint:md": "echo 'TODO'; exit 1",
    "lint:ts": "tsc --noEmit",
    "test": "jest --coverage --ci"
  },
  "workspaces": {
    "packages": [
      "packages/ekscss",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@types/node": "14.14.37",
    "@typescript-eslint/eslint-plugin": "4.18.0",
    "@typescript-eslint/parser": "4.18.0",
    "eslint": "7.22.0",
    "eslint-config-airbnb-typescript": "12.3.1",
    "eslint-plugin-import": "2.22.1",
    "prettier": "2.2.1",
    "prettier-plugin-package2": "0.5.2",
    "typescript": "4.2.3"
  },
  "prettier": {
    "arrowParens": "always",
    "endOfLine": "lf",
    "singleQuote": true,
    "trailingComma": "all",
    "overrides": [
      {
        "files": ".yarnrc.yml",
        "options": {
          "singleQuote": false
        }
      }
    ]
  },
  "renovate": {
    "extends": [
      "@wearegenki"
    ]
  }
}
