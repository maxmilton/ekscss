{
  "name": "@ekscss/docs",
  "version": "0.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn prebuild && NODE_ENV=production yarn node build.mjs && yarn postbuild",
    "dev": "NODE_ENV=development yarn node build.mjs",
    "postbuild": "tsc --emitDeclarationOnly",
    "prebuild": "rm -rf dist; mkdir -p dist/plugins"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@ekscss/framework": "workspace:*",
    "marked": "^2.0.3",
    "stage0": "^0.0.25"
  },
  "devDependencies": {
    "@types/marked": "2.0.2",
    "esbuild": "0.11.9",
    "esbuild-minify-templates": "0.0.3",
    "esbuild-plugin-ekscss": "workspace:*",
    "typescript": "4.2.4"
  }
}
