{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "noEmit": false,

    // Fixes svelte types import
    "resolvePackageJsonExports": false
  }
}
