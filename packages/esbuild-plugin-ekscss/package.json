{
  "name": "esbuild-plugin-ekscss",
  "version": "0.0.12",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run prebuild && tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "prebuild": "rm -rf dist"
  },
  "peerDependencies": {
    "ekscss": "workspace:^",
    "esbuild": "^0.13.0 || ^0.14.0 || ^0.15.0 || ^0.16.0"
  },
  "dependencies": {
    "joycon": "^3.1.0"
  },
  "devDependencies": {
    "ekscss": "workspace:*",
    "typescript": "4.9.4"
  }
}
